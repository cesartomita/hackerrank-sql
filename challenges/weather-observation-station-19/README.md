# Weather Observation Station 19

Consider **P1(a, c)** and **P2(b, d)** to be two points on a 2D plane, where:

- `a` and `b` are the respective minimum and maximum values of **LAT_N** in **STATION**
- `c` and `d` are the respective minimum and maximum values of **LONG_W** in **STATION**

Query the Euclidean Distance between points **P1** and **P2** and format your answer to display `4` decimal digits.

Input Format

The **STATION** table is described as follows:

![1775235895510](image/README/1775235895510.png)

where **LAT_N** is the northern latitude and **LONG_W** is the western longitude.